import styled from 'styled-components';

export const PageContainer = styled.div`
  position: absolute;
  left: 0;
  top: 0;

  display: flex;
  flex-direction: row;

  width: 100vw;
  min-height: 100vh;

  overflow: hidden;

  background: #ccc;
`;
