export const root = '/';
export const auth = `${root}Auth`;
export const profile = `${root}profile`;
export const forum = `${root}forum`;

export const group = `${root}group`;
export const createGroup = `${group}/create`;
export const editGroup = `${group}/edit`;

export const players = `${root}players`;
export const createPlayer = `${players}/create`;
export const editPlayer = `${players}/edit`;

export const person = `${root}person`;
export const editPerson = `${person}/edit`;

export const faction = `${root}faction`;
export const editFaction = `${faction}/editFaction`;
export const createFaction = `${faction}/createFaction`;

export const adminLevel = `${root}adminLevel`;
export const createAdminLevel = `${adminLevel}/create`;
export const editAdminLevel = `${adminLevel}/edit`;

export const log = `${root}/log`;
export const testRoom = `${root}/testRoom`;
